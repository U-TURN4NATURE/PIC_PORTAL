[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma generate && node dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
